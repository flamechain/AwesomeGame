#ifndef GENERATION_H
#define GENERATION_H

#include "utils.h"
#include "level.h"

/// Generates a random level
/// @param x    x grid size
/// @param y    y grid size
Level GenerateRandomLevel(int x, int y);

#endif
